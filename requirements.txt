# Runtime dependencies for Draft 1.
# Versions below are recent-as-of-writing pins — run `pip list --outdated`
# (or set up pip-tools/uv) before first install to confirm nothing newer
# and non-breaking has shipped since.

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==10.4.0
rapidfuzz==3.9.7
openpyxl==3.1.5
pandas==2.2.3
python-multipart==0.0.9
jinja2==3.1.4

# OCR backend: PP-OCRv6 (PaddleOCR), per the research-agent comparison.
# paddleocr does not depend on paddlepaddle -- the inference framework is a
# separate package and must be pinned alongside it. Neither is imported
# outside app/ocr/paddle.py -- the rest of the app goes through
# app/ocr/base.py's Recognizer interface instead.
paddleocr==3.7.0
paddlepaddle==3.3.1
